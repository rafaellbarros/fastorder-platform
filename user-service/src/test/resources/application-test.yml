# src/test/resources/application-test.yml
spring:
  application:
    name: user-service-test

  # Configuração de segurança para testes
  security:
    enabled: false  # Desabilitar segurança em testes
