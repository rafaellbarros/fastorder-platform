server:
  port: 0  # porta dinâmica

spring:
  application:
    name: user-service

  output:
    ansi:
      enabled: ALWAYS

eureka:
  client:
    service-url:
      defaultZone: http://discovery-server:8761/eureka
  instance:
    prefer-ip-address: true
